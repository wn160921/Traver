<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/mylv</item>
        <item name="colorPrimaryDark">@color/mylv</item>
        <item name="colorAccent">@color/mimahuang</item>
    </style>

</resources>
